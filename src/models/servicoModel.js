class Servico {
  constructor(id, descricao, dt_servico, id_categoria, valor) {
    this.id = id;
    this.descricao = descricao;
    this.dt_servico = dt_servico;
    this.id_categoria = id_categoria;
    this.valor = valor;
  }

  get() {}

  set() {}
}

export default Servico;
